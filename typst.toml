[package]
name = "sheet-music"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["justinbornais"]
license = "MIT"
description = "Render professional sheet music directly in Typst documents."
repository = "https://github.com/justinbornais/typst-sheet-music"
keywords = ["music", "notation", "sheet-music", "score"]
categories = ["visualization", "components"]
disciplines = ["music"]
compiler = "0.14.0"

[dependencies]
cetz = "0.4.2"
